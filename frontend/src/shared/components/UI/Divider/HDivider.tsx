import { FC } from 'react';
import css from './Divider.module.css';

interface IProps { }

const HDivider: FC<IProps> = () => {
  return (
    <div className={css.wrapper}>

    </div>
  );
};

export default HDivider;