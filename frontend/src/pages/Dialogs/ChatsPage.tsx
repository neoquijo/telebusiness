import { FC } from 'react';
import css from './ChatsPage.module.css';

interface IProps { }

const DialogsPage: FC<IProps> = () => {
  return (
    <div className={css.wrapper}>

    </div>
  );
};

export default DialogsPage;